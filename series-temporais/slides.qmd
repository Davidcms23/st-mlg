---
title: "Séries de Voos: Modelagem e Previsão com ETS e ARIMA"
subtitle: "Análise da série temporal de voos"
author: "Adryãn e Christian"
format:
  revealjs:
    slide-number: true
    transition: fade
    toc: false
editor: visual
---

```{r}
#| label: preparacao
#| echo: false
#| message: false
#| warning: false

rm(list = ls()); gc(full = TRUE)

library(tidyverse)
library(fpp3)
library(fpp2)
library(urca)
library(tseries)
library(conflicted)

conflicts_prefer(dplyr::select)
conflicts_prefer(dplyr::filter)
conflicts_prefer(dplyr::lag)
conflicts_prefer(fabletools::report)

dados <- readRDS("dados/Dados_Estatisticos.rds")
dados <- dados |> filter(ANOMES < yearmonth("2020 jan"))
dados <- dados |> dplyr::ungroup()

treinamento <- dados |> head(216)
teste <- dados |> tail(nrow(dados) - 216)
```

# Introdução

## Contextualização

## Objetivos

# Metodologia

## Modelos utilizados: ETS e ARIMA

## Ajuste dos modelos

## Seleção do modelo: AIC e REQM(RMES)

# Resultados

## Análise descritiva

```{r}
#| label: descritiva
#| echo: false

dados |> autoplot()
```

## Comportamento sazonal

```{r}
#| label: sazonalidade
#| echo: false

dados |> gg_season()
```

## Autocorrelação

```{r}
#| label: acf-inicial
#| echo: false

dados |> ACF(lag_max = 42) |> autoplot()
```

## Ajuste do modelo ETS

```{r}
#| label: ets-ajuste
#| echo: false

ajustes_reduzidos <- treinamento |>
  model(
    mna = ETS(n ~ error("M") + trend("N") + season("A")),
    mnm = ETS(n ~ error("M") + trend("N") + season("M")),
    ana = ETS(n ~ error("A") + trend("N") + season("A")),
  )

ajustes_reduzidos |> 
  report() |>
  select(.model, AICc, BIC, sigma2) |> 
  arrange(AICc)
```

## Estacionaridade da série

```{r}
#| label: estacionaridade
#| echo: false

dados |> 
  features(n, list(unitroot_nsdiffs, unitroot_ndiffs))
```

## Uma diferença sazonal vs. duas diferenças

```{r}
#| label: diferencas-juntas
#| echo: false

dados |> 
  mutate(
    d_sazonal = difference(n, 12),
    d_dupla   = difference(difference(n, 1), 12)
  ) |> 
  pivot_longer(c(d_sazonal, d_dupla), names_to = "serie", values_to = "valor") |> 
  as_tsibble(index = ANOMES, key = serie) |> 
  autoplot(valor)
```

## Uma diferença sazonal

```{r}
#| label: diferenca-sazonal
#| echo: false

dados |> autoplot(difference(n, 12))
```

## Duas diferenças (simples + sazonal)

```{r}
#| label: diferenca-dupla
#| echo: false

dados |> autoplot(difference(difference(n, 1), 12))
```

## Comparação: uma vs. duas diferenças (RMSE)

```{r}
#| label: comparacao-diferencas
#| echo: false

modelos_arima_todos <- treinamento |> 
  model(
    auto1   = ARIMA(n ~ pdq(1, 1, 1) + PDQ(2, 0, 0)),
    auto2   = ARIMA(n),
    arima1  = ARIMA(n ~ pdq(1,1,1) + PDQ(2, 1, 1)),
    arima2  = ARIMA(n ~ pdq(0,1,1) + PDQ(2, 1, 1)),
    best2   = ARIMA(n ~ 0 + pdq(2, 0, 2) + PDQ(2, 1, 2)),
    best    = ARIMA(n ~ 0 + pdq(2, 0, 1) + PDQ(2, 1, 2)),
    best2ad = ARIMA(n ~ 0 + pdq(2, 1, 2) + PDQ(2, 1, 2)),
    auto3   = ARIMA(n ~ pdq(0, 1, 2) + PDQ(2, 1, 2))
  )

fc_arimas_todos <- modelos_arima_todos |> 
  forecast(h = "2 years")

# extrai a ordem (p,d,q)(P,D,Q) de cada modelo ajustado
ordens <- modelos_arima_todos |> 
  pivot_longer(everything(), names_to = ".model", values_to = "ajuste") |> 
  mutate(ordem = map_chr(ajuste, ~ model_sum(.x))) |> 
  as_tibble() |> 
  select(.model, ordem)

accuracy(fc_arimas_todos, dados) |> 
  left_join(ordens, by = ".model") |> 
  select(.model, ordem, RMSE) |> 
  arrange(RMSE)
```

## ACF e PACF do modelo escolhido

```{r}
#| label: acf-pacf-escolhido
#| echo: false

dados |> 
  gg_tsdisplay(
    difference(n, 1) |> difference(12),
    plot_type = "partial",
    lag_max = 36
  )
```

## Ajuste do modelo ARIMA

```{r}
#| label: arima-ajuste
#| echo: false

modelos_reduzidos <- treinamento |> 
  model(
    arima1  = ARIMA(n ~ pdq(1,1,1) + PDQ(2, 1, 1)),
    arima2  = ARIMA(n ~ pdq(0,1,1) + PDQ(2, 1, 1)),
    best2ad = ARIMA(n ~ 0 + pdq(2, 1, 2) + PDQ(2, 1, 2)),
    auto3   = ARIMA(n ~ pdq(0, 1, 2) + PDQ(2, 1, 2))
  )

ordens <- modelos_reduzidos |> 
  pivot_longer(everything(), names_to = ".model", values_to = "ajuste") |> 
  mutate(ordem = map_chr(ajuste, model_sum)) |> 
  as_tibble() |> 
  select(.model, ordem)

modelos_reduzidos |> 
  glance() |> 
  left_join(ordens, by = ".model") |> 
  select(.model, ordem, AICc, BIC, sigma2) |> 
  arrange(AICc)
```

::: {.notes}
Aqui o AICc é válido: todos os modelos têm d=1 e D=1 (mesma ordem de diferenciação),
diferindo apenas em p, q, P, Q.
auto3 venceu com o menor AICc.
:::

## Por que ARIMA(0,1,2)(2,1,2)?

::: {.incremental}
- RMSE definiu a diferenciação
- Menor AICc entre os candidatos
- Por que usar o AICc como críterio de decisão?
- Mesma diferenciação (d=1, D=1)
- Penaliza modelos excessivamente complexos (parcimônia)
:::

## Seleção do modelo: acurácia fora da amostra

```{r}
#| label: comparacao-final
#| echo: false

melhor_modelo_arima <- modelos_reduzidos |> select(arima1)
melhor_modelo_ets <- ajustes_reduzidos |> select(mna)

ajuste_final <- treinamento |> 
  model(
    arima1 = ARIMA(n ~ pdq(1,1,1) + PDQ(2, 1, 1)),
    mna = ETS(n ~ error("M") + trend("N") + season("A"))
  )

fc_final <- ajuste_final |> forecast(h = "2 years")

accuracy(fc_final, dados) |> arrange(RMSE)
```

## Diagnóstico dos resíduos

```{r}
#| label: residuos
#| echo: false

melhor_modelo_arima |> 
  gg_tsresiduals(lag_max = 36)
```

## Teste de Ljung-Box e Shapiro-Wilk

```{r}
#| label: testes-residuos
#| echo: false

modelos_reduzidos |> augment() |>
  filter(.model == "arima1") |> 
  features(.innov, ljung_box, lag = 24, dof = 5)

shapiro.test((melhor_modelo_arima |> augment())$.innov)
```

## Previsão

```{r}
#| label: previsao-final
#| echo: false

melhor_modelo_geral <- dados |> 
  model(
    arima1 = ARIMA(n ~ pdq(1,1,1) + PDQ(2, 1, 1))
  )

melhor_modelo_geral |> 
  forecast(h = "4 years") |> 
  autoplot(dados)
```

# Conclusões

## Síntese dos resultados

## Sugestões para trabalhos futuros

# Referências